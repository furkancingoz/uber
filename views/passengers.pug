doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Homepage 
        style.
         body {
           font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
              }
    body
    h1 Welcome to taxi booking app
    p1 With this app, you can book a taxi from one place to anatoher.

    h2 Passengers 
    p The are #{passengers.length} passengers waitings.

    ol
       each passenger in passengers
         li
           a(href=`/passengers/${passenger.id}`)= passenger.name 
         